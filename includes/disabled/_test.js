function getBuiltInTypes(psCurLang) {
	var retVal = new ActiveXObject("Scripting.Dictionary");
	if(psCurLang == "js") {
		retVal.Add(,)
	}
	return retVal;
}





function tests() {
	d = new Number(20);
	var retVal = new ActiveXObject("Scripting.Dictionary");
	var wshShell = new ActiveXObject("WScript.Shell");
    var form = document.forms["newmsg_form"];
	var funcRT = getBuiltInTypes("js");
	// тестовые строки, добавьте точку для тестирования....
	// НЕ Реализованные ;(
	wshShell.	 	// Надо реализовать логику скрипта intsOLEGenerator.vbs - генератор *.ints файлов для Intellisence
	funcRT.			//  Надо реазизовать парсинг функции и определение типа возвращаемого результата.	
	form.			// DHTML..  надо курить
	document.		// DHTML..  см. выше
	
	// Реализованные, блок определений
	// Надо реализовать разбор  tDate.toLocaleString(); c определением типа tDate и что возвращеает toLocaleString();
	var re = new RegExp(patern, 'img');
	var tDate = new Date;
	var tDateStr = tDate.toLocaleString();
	// Реализованные, блок для тестов.
	tDate.					//.getDate()
	ttt = tDateStr.split()  	//split();
	ttt.					//length
	retVal.
	d.
	Number.
	re.
	// jN типы
	Editor.
}
fopen()


function test_one(){
	//debugger;
	var vText = "Первая строка \n вторая строка \n Третья строка";
	var vTextLines = vText.split('\n');
	var vTLLen = vTextLines.length-1;
	for (iL = vTLLen; iL>=0; iL-- ) {
		var tLine = vTextLines[iL];
		tLine.
		alert(tLine);
	}
}
//test_one();
