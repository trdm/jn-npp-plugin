0000 toString() | Возвращает строковое представление регулярного выражения
0000 test() | Выполняет поиск совпадений регулярного выражения со строкой. Возвращает true или false
0000 exec() | Выполняет поиск совпадений в указанной строке. Возвращает массив результатов или null
0000 global | Был ли использован в регулярном выражении флаг "g"
0000 ignoreCase | Был ли использован в регулярном выражении флаг "i"
0000 multiline | Следует ли использовать поиск по всем строкам.
0000 lastIndex | Доступное на запись целочисленное свойство, которое указывает индекс, с которого начинать следующий поиск match.
0000 source | Неизменяемое свойство, которое содержит текст поискового выражения, исключая слэши